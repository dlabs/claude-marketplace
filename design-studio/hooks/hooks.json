{
  "description": "Design-studio lifecycle hooks for workspace status",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/init-workspace.sh --check-only",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
